# AI 测试总控 Prompt


---

你现在是这个项目的测试负责人。请对当前仓库执行一次“尽可能自动化、可复现、可落地”的完整测试，并把发现的问题整理成文档，方便开发者逐条修复。

## 目标

1. 先阅读项目结构，理解它的核心模块、启动方式、GUI/CLI 主流程、文件读写路径和高风险操作。
2. 能自动化的优先自动化，直接编写并运行测试代码。
3. 不能自动化的部分，明确列出手工测试步骤，不要只说“手动点一点看看”。
4. 每发现一个 bug，都要写进 Markdown 文档，包含复现方式、实际结果、预期结果、影响范围、修复建议。
5. 修 bug 时不要顺手做无关重构，除非不改就没法稳定测试。

## 你要重点覆盖的范围

### 1. 启动与基础可用性
- `desktop_agent_gui.py` GUI 启动
- `desktop_agent_cli.py` CLI 帮助和命令分发
- `config.json` 缺失 / 默认配置生成
- 日志初始化
- 页面切换是否报错

### 2. 核心业务链路
- 扫描桌面
- 生成整理计划
- 生成人工审核文件
- Dryrun 预演
- Apply 执行
- Undo 撤销
- 计划解释生成
- 整理记忆读写

### 3. Windows 特有逻辑
- 当前用户桌面 + 公用桌面合并扫描
- 快捷方式分类目标路径
- `os.startfile` / `explorer` 打开目录逻辑
- Windows 路径与中文路径兼容性

### 4. GUI 稳定性
- 首页扫描后跳转
- 整理方案页加载
- 设置页保存 / 导入 / 导出
- 计划解释页展示
- 整理记忆页增删改存
- 运行日志页按钮
- 帮助页更新检查

### 5. 边界与异常
- 缺少输入文件时的提示
- 非法配置
- 空数据
- 路径不存在
- 目标目录已存在重名文件
- 用户取消操作
- 模型未下载 / 无网络 / 更新源异常

## 执行要求

1. 先查看仓库里是否已有：
   - `tests/`
   - `tools/run_test_audit.py`
   - `test_reports/latest_test_report.md`

2. 先运行现有基线测试：

```bash
python tools/run_test_audit.py
```

3. 如果测试覆盖不够，请继续补充自动化测试，优先使用：
   - `unittest`
   - `tempfile`
   - `unittest.mock`

4. 为了避免污染真实桌面或真实配置，测试时优先：
   - 使用临时目录
   - mock 文件打开、目录打开、网络请求
   - mock `load_config()`、`os.startfile()`、`subprocess.Popen()`

5. 自动化测试完成后，请输出：
   - 新增/修改了哪些测试文件
   - 哪些测试通过
   - 哪些测试失败
   - 新发现的 bug 列表
   - 哪些部分必须人工测试

## Bug 文档要求

如果自动化测试失败，或者你手工测试发现问题，请把问题写到：

`test_reports/latest_test_report.md`

每个 bug 至少包含：

- 标题
- 影响模块
- 复现步骤
- 实际结果
- 预期结果
- 严重程度
- 修复建议

## 人工测试要求

如果某部分不适合自动化，请给出明确手工步骤，例如：

1. 打开 GUI
2. 点击“扫描”
3. 检查是否自动跳转到“整理方案”
4. 修改一条分类并保存
5. 回到首页点击“确认并整理”
6. 检查目标目录是否符合预期

不要只写“人工确认页面正常”。

## 输出风格

- 优先给结论
- 再列 bug
- 最后列测试覆盖缺口
- 不要泛泛而谈
- 不要只给建议，不写实际执行结果

---

如果你发现某部分无法自动化，请明确说明“为什么不能自动化”和“建议如何人工测试”。
