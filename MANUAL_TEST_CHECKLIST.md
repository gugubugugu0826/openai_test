# 手工测试清单

下面这些内容目前更适合在 Windows 真机上手工验证：

## 1. 真实桌面扫描
1. 在当前用户桌面放 3 个测试文件、1 个文件夹、1 个快捷方式。
2. 在 `C:\Users\Public\Desktop` 再放 1 个公共快捷方式。
3. 启动 GUI，点击“扫描”。
4. 确认扫描完成后自动跳到“整理方案”。
5. 确认方案里同时出现当前用户桌面和公用桌面的项目。

## 2. 整理执行
1. 在设置页把目标目录设为一个全新的测试目录。
2. 扫描并生成方案。
3. 在“整理方案”里手动修改 1 个分类，跳过 1 个项目。
4. 执行 Dryrun，检查日志输出是否合理。
5. 执行 Apply。
6. 检查目标目录结构、文件去向、重名文件处理是否正确。

## 3. 撤销
1. Apply 后执行 Undo。
2. 检查被移动的文件是否返回原位置。
3. 检查复制的文件夹是否没有被自动删除，并且日志提示准确。

## 4. 整理记忆
1. 进入“整理记忆”页。
2. 新增 1 条规则，例如“简历 -> 简历求职”。
3. 保存并关闭程序。
4. 重新打开程序，检查规则是否仍然存在。
5. 用一个包含该关键词的文件重新扫描，检查是否命中该分类。

## 5. 更新检查
1. 在设置里填 GitHub Releases 的 `latest` API 地址。
2. 把本地版本号设置得低于线上版本。
3. 启动程序，确认是否自动检查更新。
4. 手动点击“检查更新”，确认是否可下载更新包。
5. 下载完成后确认 `updates/` 目录下文件完整。

## 6. 打包发布版
1. 运行 `python build_release.py`。
2. 用发布包中的 exe 在另一目录启动。
3. 检查首次运行、模型获取、扫描、整理、日志、更新检查是否正常。
